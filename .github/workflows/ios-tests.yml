name: iOS Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  app-tests:
    name: App Tests
    runs-on: macos-latest
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      
    - name: Select Xcode
      run: sudo xcode-select -s /Applications/Xcode_16.2.app/Contents/Developer
      
    - name: Check Environment
      run: |
        sw_vers
        xcrun --sdk macosx --show-sdk-version
        xcodebuild -version
        
    - name: List Xcode Test Schemes
      run: |
        xcodebuild -list -project TypeBeat.xcodeproj || echo "Failed to list schemes"
        
    - name: Build Project
      run: |
        # Just build the project without running tests
        xcodebuild build -project TypeBeat.xcodeproj -scheme "TypeBeat" -destination "platform=macOS,variant=Mac Catalyst" MACOSX_DEPLOYMENT_TARGET=14.7 CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO 