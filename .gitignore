.DS_Store

# Xcode user-specific state — UI layout, breakpoints, scheme management.
# Tied to a single developer's machine; should not be in version control.
xcuserdata/
*.xcuserstate

# Local build / derived-data output (e.g. xcodebuild -derivedDataPath build).
build/
